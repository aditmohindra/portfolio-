---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center border border-border-muted bg-canvas px-2 py-0.5 text-micro uppercase tracking-wide text-ink-muted',
    className,
  ]}
>
  <slot />
</span>
